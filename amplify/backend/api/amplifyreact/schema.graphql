type Todo
  @model
  # 自分が作成したItemsのみ表示できるように
  @auth(rules: [{ allow: owner }])
  @key(
    name: "SortByName"
    fields: ["owner", "name"]
    queryField: "listTodosSortedByName"
  ) {
  id: ID!
  name: String!
  owner: String
  updatedAt: AWSDateTime
  createdAt: AWSDateTime
}
